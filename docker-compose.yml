services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: lotline
      POSTGRES_USER: lotline
      POSTGRES_PASSWORD: lotline
    ports:
      - "5432:5432"
    volumes:
      - lotline-postgres:/var/lib/postgresql/data

volumes:
  lotline-postgres:
